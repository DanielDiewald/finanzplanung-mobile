Capyt 2.2.2a – Test Results
===========================

Baseline (unveraenderter hochgeladener Stand 2.2.1a)
---------------------------------------------------
`npm test`: 60 Tests insgesamt, 55 bestanden, 5 fehlgeschlagen.
`npm run check`: fehlgeschlagen, weil das Root-Manifest `./web-app-manifest-192x192.png` erwartet, die vorhandenen Manifest-Icons aber unter `assets/favicons/` liegen.

Die fuenf Baseline-Testfehler sind:
- Desktop-Monatsvisualisierung erwartet den alten Default `monthVisualMode: "buffer"`.
- Mobile-Monatsvisualisierung erwartet den alten Default `visualMode = 'buffer'`.
- Mobile-Storage-Test erwartet den alten Default `selectedMonthVisualMode:'buffer'`.
- PWA-Updater-Test erwartet `registration.update()`.
- PWA-UI-Test ist auf den alten Script-Pfad `js/pwa-update.js?v=2.1.0` fest verdrahtet.

Aktueller Stand 2.2.2a
----------------------
`npm test`: 71 Tests insgesamt, 66 bestanden, 5 fehlgeschlagen.
Die fuenf Fehler sind exakt dieselben bereits im unveraenderten Ausgangsstand vorhandenen Tests. Es wurde kein bestehender Test geloescht oder deaktiviert.

Neue/aktualisierte Capy-/FP1-Regressionstests
--------------------------------------------
29/29 gezielte Capy- und Desktop-FP1-Tests bestanden. Geprueft werden unter anderem:
- Character Creator: maennlich/weiblich, temporaere Auswahl bis `Capy erstellen`, kein `divers`
- direkte Pointer-Streichelmechanik: Mindestdistanz, Cooldown, Happiness-Cap und Session-Limit
- Pointer-Inventar-Drag, Drag-Ghost, Drop-Zone und `pointercancel`
- Shop ohne direkte Fuettern-Aktion sowie Item-Typen `food`, `toy`, `care`
- generische Item-Effekte aus `items.json`
- Mobile-Viewport/Safe-Area/Bottom-Sheet-Struktur
- Pause erhaelt Game-State und wird ueber FP1 synchronisiert
- abgelehnte Capy-Buchungen liefern `rejected` zurueck und sperren die gekoppelte Coin-Gutschrift
- FP1-P transportiert `transactionResults` inklusive Ablehnungsgrund
- Budgetmigration `Momo Vorrat` -> `Momo's Vorrat` ohne Duplikat und ohne Verlust bestehender Bezüge
- bestehende Vorrat-Sperren, interne Umbuchungen, Coin-Deduplizierung, Dark-Mode-Bridge und FP1-Kompatibilitaet

Statische/Strukturpruefungen
----------------------------
- Alle geaenderten JavaScript-/MJS-Dateien syntaktisch gueltig (`node --check`).
- `git diff --check` ohne Whitespace-Fehler.
- Relevante JSON-Dateien parsebar.
- Alle 77 in `sw.js` referenzierten lokalen CORE-Pfade sind vorhanden.
- Der vorhandene `npm run check` scheitert weiterhin ausschließlich am bereits im Baseline-Stand vorhandenen Manifest-Pfadfehler.
